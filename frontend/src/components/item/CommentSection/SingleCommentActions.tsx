import { Delete } from "@mui/icons-material";
import { IconButton } from "@mui/material";

export function SingleCommentActions() {
  return (
    <IconButton>
      <Delete />
    </IconButton>
  );
}
